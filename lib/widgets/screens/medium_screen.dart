import 'package:flutter/material.dart';
import '../cards/cards_medium.dart';

class MediumScreen extends StatelessWidget {
  const MediumScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CardsMedium();
  }
}
